#include "vision/processors/cuda/draw_gpu.cuh"
#include <cuda_runtime.h>
#include <cstring>

namespace modeldeploy::vision {
    // 公共领域 8x16 VGA 字体位图（ASCII 0x20-0x7E），来自 dhepper/font8x8 (font8x16_basic, CC0)
    __constant__ uint8_t FONT8X16[95][16] = {
        /* ' ' 0x20 */
        {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '!' 0x21 */
        {0x00,0x00,0x18,0x3C,0x3C,0x3C,0x18,0x18,0x18,0x00,0x18,0x18,0x00,0x00,0x00,0x00},
        /* '"' 0x22 */
        {0x00,0x66,0x66,0x66,0x66,0x24,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '#' 0x23 */
        {0x00,0x00,0x00,0x6C,0x6C,0xFE,0x6C,0x6C,0x6C,0xFE,0x6C,0x6C,0x00,0x00,0x00,0x00},
        /* '$' 0x24 */
        {0x18,0x18,0x7C,0xC6,0xC2,0xC0,0x7C,0x06,0x06,0x86,0xC6,0x7C,0x18,0x18,0x00,0x00},
        /* '%' 0x25 */
        {0x00,0x00,0x00,0x00,0xC2,0xC6,0x0C,0x18,0x30,0x60,0xC6,0x86,0x00,0x00,0x00,0x00},
        /* '&' 0x26 */
        {0x00,0x00,0x38,0x6C,0x6C,0x38,0x76,0xDC,0xCC,0xCC,0xCC,0x76,0x00,0x00,0x00,0x00},
        /* '\'' 0x27 */
        {0x00,0x30,0x30,0x30,0x60,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '(' 0x28 */
        {0x00,0x00,0x0C,0x18,0x30,0x30,0x30,0x30,0x30,0x30,0x18,0x0C,0x00,0x00,0x00,0x00},
        /* ')' 0x29 */
        {0x00,0x00,0x30,0x18,0x0C,0x0C,0x0C,0x0C,0x0C,0x0C,0x18,0x30,0x00,0x00,0x00,0x00},
        /* '*' 0x2A */
        {0x00,0x00,0x00,0x00,0x00,0x66,0x3C,0xFF,0x3C,0x66,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '+' 0x2B */
        {0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x7E,0x18,0x18,0x00,0x00,0x00,0x00,0x00,0x00},
        /* ',' 0x2C */
        {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x18,0x30,0x00,0x00},
        /* '-' 0x2D */
        {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x7E,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '.' 0x2E */
        {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x00,0x00,0x00,0x00},
        /* '/' 0x2F */
        {0x00,0x00,0x00,0x00,0x02,0x06,0x0C,0x18,0x30,0x60,0xC0,0x80,0x00,0x00,0x00,0x00},
        /* '0' 0x30 */
        {0x00,0x00,0x38,0x6C,0xC6,0xC6,0xD6,0xD6,0xC6,0xC6,0x6C,0x38,0x00,0x00,0x00,0x00},
        /* '1' 0x31 */
        {0x00,0x00,0x18,0x38,0x78,0x18,0x18,0x18,0x18,0x18,0x18,0x7E,0x00,0x00,0x00,0x00},
        /* '2' 0x32 */
        {0x00,0x00,0x7C,0xC6,0x06,0x0C,0x18,0x30,0x60,0xC0,0xC6,0xFE,0x00,0x00,0x00,0x00},
        /* '3' 0x33 */
        {0x00,0x00,0x7C,0xC6,0x06,0x06,0x3C,0x06,0x06,0x06,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* '4' 0x34 */
        {0x00,0x00,0x0C,0x1C,0x3C,0x6C,0xCC,0xFE,0x0C,0x0C,0x0C,0x1E,0x00,0x00,0x00,0x00},
        /* '5' 0x35 */
        {0x00,0x00,0xFE,0xC0,0xC0,0xC0,0xFC,0x06,0x06,0x06,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* '6' 0x36 */
        {0x00,0x00,0x38,0x60,0xC0,0xC0,0xFC,0xC6,0xC6,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* '7' 0x37 */
        {0x00,0x00,0xFE,0xC6,0x06,0x06,0x0C,0x18,0x30,0x30,0x30,0x30,0x00,0x00,0x00,0x00},
        /* '8' 0x38 */
        {0x00,0x00,0x7C,0xC6,0xC6,0xC6,0x7C,0xC6,0xC6,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* '9' 0x39 */
        {0x00,0x00,0x7C,0xC6,0xC6,0xC6,0x7E,0x06,0x06,0x06,0x0C,0x78,0x00,0x00,0x00,0x00},
        /* ':' 0x3A */
        {0x00,0x00,0x00,0x18,0x18,0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x00,0x00,0x00,0x00},
        /* ';' 0x3B */
        {0x00,0x00,0x00,0x18,0x18,0x00,0x00,0x00,0x00,0x00,0x18,0x18,0x30,0x00,0x00,0x00},
        /* '<' 0x3C */
        {0x00,0x00,0x00,0x06,0x0C,0x18,0x30,0x60,0x30,0x18,0x0C,0x06,0x00,0x00,0x00,0x00},
        /* '=' 0x3D */
        {0x00,0x00,0x00,0x00,0x00,0x7E,0x00,0x00,0x00,0x7E,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '>' 0x3E */
        {0x00,0x00,0x00,0x60,0x30,0x18,0x0C,0x06,0x0C,0x18,0x30,0x60,0x00,0x00,0x00,0x00},
        /* '?' 0x3F */
        {0x00,0x00,0x7C,0xC6,0xC6,0x0C,0x18,0x18,0x18,0x00,0x18,0x18,0x00,0x00,0x00,0x00},
        /* '@' 0x40 */
        {0x00,0x00,0x00,0x7C,0xC6,0xC6,0xDE,0xDE,0xDE,0xDC,0xC0,0x7C,0x00,0x00,0x00,0x00},
        /* 'A' 0x41 */
        {0x00,0x00,0x10,0x38,0x6C,0xC6,0xC6,0xFE,0xC6,0xC6,0xC6,0xC6,0x00,0x00,0x00,0x00},
        /* 'B' 0x42 */
        {0x00,0x00,0xFC,0x66,0x66,0x66,0x7C,0x66,0x66,0x66,0x66,0xFC,0x00,0x00,0x00,0x00},
        /* 'C' 0x43 */
        {0x00,0x00,0x3C,0x66,0xC2,0xC0,0xC0,0xC0,0xC0,0xC2,0x66,0x3C,0x00,0x00,0x00,0x00},
        /* 'D' 0x44 */
        {0x00,0x00,0xF8,0x6C,0x66,0x66,0x66,0x66,0x66,0x66,0x6C,0xF8,0x00,0x00,0x00,0x00},
        /* 'E' 0x45 */
        {0x00,0x00,0xFE,0x66,0x62,0x68,0x78,0x68,0x60,0x62,0x66,0xFE,0x00,0x00,0x00,0x00},
        /* 'F' 0x46 */
        {0x00,0x00,0xFE,0x66,0x62,0x68,0x78,0x68,0x60,0x60,0x60,0xF0,0x00,0x00,0x00,0x00},
        /* 'G' 0x47 */
        {0x00,0x00,0x3C,0x66,0xC2,0xC0,0xC0,0xDE,0xC6,0xC6,0x66,0x3A,0x00,0x00,0x00,0x00},
        /* 'H' 0x48 */
        {0x00,0x00,0xC6,0xC6,0xC6,0xC6,0xFE,0xC6,0xC6,0xC6,0xC6,0xC6,0x00,0x00,0x00,0x00},
        /* 'I' 0x49 */
        {0x00,0x00,0x3C,0x18,0x18,0x18,0x18,0x18,0x18,0x18,0x18,0x3C,0x00,0x00,0x00,0x00},
        /* 'J' 0x4A */
        {0x00,0x00,0x1E,0x0C,0x0C,0x0C,0x0C,0x0C,0xCC,0xCC,0xCC,0x78,0x00,0x00,0x00,0x00},
        /* 'K' 0x4B */
        {0x00,0x00,0xE6,0x66,0x66,0x6C,0x78,0x78,0x6C,0x66,0x66,0xE6,0x00,0x00,0x00,0x00},
        /* 'L' 0x4C */
        {0x00,0x00,0xF0,0x60,0x60,0x60,0x60,0x60,0x60,0x62,0x66,0xFE,0x00,0x00,0x00,0x00},
        /* 'M' 0x4D */
        {0x00,0x00,0xC6,0xEE,0xFE,0xFE,0xD6,0xC6,0xC6,0xC6,0xC6,0xC6,0x00,0x00,0x00,0x00},
        /* 'N' 0x4E */
        {0x00,0x00,0xC6,0xE6,0xF6,0xFE,0xDE,0xCE,0xC6,0xC6,0xC6,0xC6,0x00,0x00,0x00,0x00},
        /* 'O' 0x4F */
        {0x00,0x00,0x7C,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'P' 0x50 */
        {0x00,0x00,0xFC,0x66,0x66,0x66,0x7C,0x60,0x60,0x60,0x60,0xF0,0x00,0x00,0x00,0x00},
        /* 'Q' 0x51 */
        {0x00,0x00,0x7C,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xD6,0xDE,0x7C,0x0C,0x0E,0x00,0x00},
        /* 'R' 0x52 */
        {0x00,0x00,0xFC,0x66,0x66,0x66,0x7C,0x6C,0x66,0x66,0x66,0xE6,0x00,0x00,0x00,0x00},
        /* 'S' 0x53 */
        {0x00,0x00,0x7C,0xC6,0xC6,0x60,0x38,0x0C,0x06,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'T' 0x54 */
        {0x00,0x00,0x7E,0x7E,0x5A,0x18,0x18,0x18,0x18,0x18,0x18,0x3C,0x00,0x00,0x00,0x00},
        /* 'U' 0x55 */
        {0x00,0x00,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'V' 0x56 */
        {0x00,0x00,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0xC6,0x6C,0x38,0x10,0x00,0x00,0x00,0x00},
        /* 'W' 0x57 */
        {0x00,0x00,0xC6,0xC6,0xC6,0xC6,0xD6,0xD6,0xD6,0xFE,0xEE,0x6C,0x00,0x00,0x00,0x00},
        /* 'X' 0x58 */
        {0x00,0x00,0xC6,0xC6,0x6C,0x7C,0x38,0x38,0x7C,0x6C,0xC6,0xC6,0x00,0x00,0x00,0x00},
        /* 'Y' 0x59 */
        {0x00,0x00,0x66,0x66,0x66,0x66,0x3C,0x18,0x18,0x18,0x18,0x3C,0x00,0x00,0x00,0x00},
        /* 'Z' 0x5A */
        {0x00,0x00,0xFE,0xC6,0x86,0x0C,0x18,0x30,0x60,0xC2,0xC6,0xFE,0x00,0x00,0x00,0x00},
        /* '[' 0x5B */
        {0x00,0x00,0x3C,0x30,0x30,0x30,0x30,0x30,0x30,0x30,0x30,0x3C,0x00,0x00,0x00,0x00},
        /* '\' 0x5C */
        {0x00,0x00,0x00,0x80,0xC0,0xE0,0x70,0x38,0x1C,0x0E,0x06,0x02,0x00,0x00,0x00,0x00},
        /* ']' 0x5D */
        {0x00,0x00,0x3C,0x0C,0x0C,0x0C,0x0C,0x0C,0x0C,0x0C,0x0C,0x3C,0x00,0x00,0x00,0x00},
        /* '^' 0x5E */
        {0x00,0x10,0x38,0x6C,0xC6,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* '_' 0x5F */
        {0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0xFF,0x00},
        /* '`' 0x60 */
        {0x00,0x30,0x18,0x0C,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00},
        /* 'a' 0x61 */
        {0x00,0x00,0x00,0x00,0x00,0x78,0x0C,0x7C,0xCC,0xCC,0xCC,0x76,0x00,0x00,0x00,0x00},
        /* 'b' 0x62 */
        {0x00,0x00,0xE0,0x60,0x60,0x78,0x6C,0x66,0x66,0x66,0x66,0x7C,0x00,0x00,0x00,0x00},
        /* 'c' 0x63 */
        {0x00,0x00,0x00,0x00,0x00,0x7C,0xC6,0xC0,0xC0,0xC0,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'd' 0x64 */
        {0x00,0x00,0x1C,0x0C,0x0C,0x3C,0x6C,0xCC,0xCC,0xCC,0xCC,0x76,0x00,0x00,0x00,0x00},
        /* 'e' 0x65 */
        {0x00,0x00,0x00,0x00,0x00,0x7C,0xC6,0xFE,0xC0,0xC0,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'f' 0x66 */
        {0x00,0x00,0x38,0x6C,0x64,0x60,0xF0,0x60,0x60,0x60,0x60,0xF0,0x00,0x00,0x00,0x00},
        /* 'g' 0x67 */
        {0x00,0x00,0x00,0x00,0x00,0x76,0xCC,0xCC,0xCC,0xCC,0xCC,0x7C,0x0C,0xCC,0x78,0x00},
        /* 'h' 0x68 */
        {0x00,0x00,0xE0,0x60,0x60,0x6C,0x76,0x66,0x66,0x66,0x66,0xE6,0x00,0x00,0x00,0x00},
        /* 'i' 0x69 */
        {0x00,0x00,0x18,0x18,0x00,0x38,0x18,0x18,0x18,0x18,0x18,0x3C,0x00,0x00,0x00,0x00},
        /* 'j' 0x6A */
        {0x00,0x00,0x06,0x06,0x00,0x0E,0x06,0x06,0x06,0x06,0x06,0x06,0x66,0x66,0x3C,0x00},
        /* 'k' 0x6B */
        {0x00,0x00,0xE0,0x60,0x60,0x66,0x6C,0x78,0x78,0x6C,0x66,0xE6,0x00,0x00,0x00,0x00},
        /* 'l' 0x6C */
        {0x00,0x00,0x38,0x18,0x18,0x18,0x18,0x18,0x18,0x18,0x18,0x3C,0x00,0x00,0x00,0x00},
        /* 'm' 0x6D */
        {0x00,0x00,0x00,0x00,0x00,0xEC,0xFE,0xD6,0xD6,0xD6,0xD6,0xC6,0x00,0x00,0x00,0x00},
        /* 'n' 0x6E */
        {0x00,0x00,0x00,0x00,0x00,0xDC,0x66,0x66,0x66,0x66,0x66,0x66,0x00,0x00,0x00,0x00},
        /* 'o' 0x6F */
        {0x00,0x00,0x00,0x00,0x00,0x7C,0xC6,0xC6,0xC6,0xC6,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 'p' 0x70 */
        {0x00,0x00,0x00,0x00,0x00,0xDC,0x66,0x66,0x66,0x66,0x66,0x7C,0x60,0x60,0xF0,0x00},
        /* 'q' 0x71 */
        {0x00,0x00,0x00,0x00,0x00,0x76,0xCC,0xCC,0xCC,0xCC,0xCC,0x7C,0x0C,0x0C,0x1E,0x00},
        /* 'r' 0x72 */
        {0x00,0x00,0x00,0x00,0x00,0xDC,0x76,0x66,0x60,0x60,0x60,0xF0,0x00,0x00,0x00,0x00},
        /* 's' 0x73 */
        {0x00,0x00,0x00,0x00,0x00,0x7C,0xC6,0x60,0x38,0x0C,0xC6,0x7C,0x00,0x00,0x00,0x00},
        /* 't' 0x74 */
        {0x00,0x00,0x10,0x30,0x30,0xFC,0x30,0x30,0x30,0x30,0x36,0x1C,0x00,0x00,0x00,0x00},
        /* 'u' 0x75 */
        {0x00,0x00,0x00,0x00,0x00,0xCC,0xCC,0xCC,0xCC,0xCC,0xCC,0x76,0x00,0x00,0x00,0x00},
        /* 'v' 0x76 */
        {0x00,0x00,0x00,0x00,0x00,0x66,0x66,0x66,0x66,0x66,0x3C,0x18,0x00,0x00,0x00,0x00},
        /* 'w' 0x77 */
        {0x00,0x00,0x00,0x00,0x00,0xC6,0xC6,0xD6,0xD6,0xD6,0xFE,0x6C,0x00,0x00,0x00,0x00},
        /* 'x' 0x78 */
        {0x00,0x00,0x00,0x00,0x00,0xC6,0x6C,0x38,0x38,0x38,0x6C,0xC6,0x00,0x00,0x00,0x00},
        /* 'y' 0x79 */
        {0x00,0x00,0x00,0x00,0x00,0x66,0x66,0x66,0x66,0x66,0x66,0x3C,0x0C,0x18,0xF0,0x00},
        /* 'z' 0x7A */
        {0x00,0x00,0x00,0x00,0x00,0xFE,0xCC,0x18,0x30,0x60,0xC6,0xFE,0x00,0x00,0x00,0x00},
        /* '{' 0x7B */
        {0x00,0x00,0x0E,0x18,0x18,0x18,0x70,0x18,0x18,0x18,0x18,0x0E,0x00,0x00,0x00,0x00},
        /* '|' 0x7C */
        {0x00,0x00,0x18,0x18,0x18,0x18,0x00,0x18,0x18,0x18,0x18,0x18,0x00,0x00,0x00,0x00},
        /* '}' 0x7D */
        {0x00,0x00,0x70,0x18,0x18,0x18,0x0E,0x18,0x18,0x18,0x18,0x70,0x00,0x00,0x00,0x00},
        /* '~' 0x7E */
        {0x00,0x00,0x76,0xDC,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00,0x00}
    };

    __device__ inline int label_bar_width(const GpuDrawBox& box) {
        int n = 0;
        while (n < 31 && box.label[n] != '\0') ++n;
        return n * 8;
    }

    __device__ __forceinline__ void set_pixel(uint8_t* __restrict__ bgr, int width,
                                              int x, int y, const GpuDrawBox& box) {
        uint8_t* p = bgr + (static_cast<size_t>(y) * width + x) * 3;
        p[0] = box.b;
        p[1] = box.g;
        p[2] = box.r;
    }

    // 1 block per box；256 线程：
    //   0-127   填充 box 内部（半透明混合，跳过标签条区域）
    //   128-255 画 2px 边框 + 顶部 8x16 标签条（标签条区域由本组独占写入，无数据竞争）
    __global__ void kernel_draw_boxes(uint8_t* __restrict__ bgr, int width, int height,
                                      const GpuDrawBox* __restrict__ boxes, int num_boxes, float alpha) {
        const int b = blockIdx.x;
        if (b >= num_boxes) return;
        const GpuDrawBox box = boxes[b];
        const int tid = threadIdx.x;

        const int x1 = max(box.x1, 0);
        const int y1 = max(box.y1, 0);
        const int x2 = min(box.x2, width);
        const int y2 = min(box.y2, height);
        if (x1 >= x2 || y1 >= y2) return;

        const int bar_w = label_bar_width(box);
        const int lx1 = x1 + 2;              // 标签条左边界（避开 2px 左边框）
        const int lx2 = lx1 + bar_w;         // 标签条右边界（不含）
        const int ly1 = y1;                  // 标签条上边界（压过顶边框）
        const int ly2 = y1 + 16;             // 标签条下边界（不含）

        const float one_minus = 1.0f - alpha;

        // ── 填充内部（半透明）──
        if (tid < 128) {
            const int ix1 = x1 + 2;
            const int iy1 = y1 + 2;
            const int ix2 = max(x2 - 2, ix1);
            const int iy2 = max(y2 - 2, iy1);
            for (int y = iy1 + tid / 16; y < iy2; y += 8) {
                const bool in_label_y = (y >= ly1 && y < ly2);
                for (int x = ix1 + (tid % 16); x < ix2; x += 16) {
                    if (in_label_y && x >= lx1 && x < lx2) continue;
                    uint8_t* p = bgr + (static_cast<size_t>(y) * width + x) * 3;
                    p[0] = static_cast<uint8_t>(p[0] * one_minus + box.b * alpha + 0.5f);
                    p[1] = static_cast<uint8_t>(p[1] * one_minus + box.g * alpha + 0.5f);
                    p[2] = static_cast<uint8_t>(p[2] * one_minus + box.r * alpha + 0.5f);
                }
            }
            return;
        }

        // ── 2px 边框（四个角归水平边；竖直边只画内部行，避免重叠写入）──
        const int t = tid - 128;   // 0..127
        if (t < 64) {
            const int side = t / 32;     // 0 顶，1 底
            const int xbase = t % 32;
            if (side == 0) {
                for (int r = 0; r < 2; ++r) {
                    const int y = y1 + r;
                    for (int x = x1 + xbase; x < x2; x += 32) {
                        if (x >= lx1 && x < lx2) continue;   // 顶边框跳过标签条
                        set_pixel(bgr, width, x, y, box);
                    }
                }
            } else {
                for (int r = 0; r < 2; ++r) {
                    const int y = y2 - 2 + r;
                    if (y >= y1 && y < y2) {
                        for (int x = x1 + xbase; x < x2; x += 32) {
                            set_pixel(bgr, width, x, y, box);
                        }
                    }
                }
            }
        } else {
            const int tt = t - 64;       // 0..63
            const int side = tt / 32;    // 0 左，1 右
            const int ybase = tt % 32;
            const int x0 = (side == 0) ? x1 : (x2 - 2);
            for (int r = 0; r < 2; ++r) {
                const int x = x0 + r;
                if (x < x1 || x >= x2) continue;
                for (int y = y1 + 2 + ybase; y < y2 - 2; y += 32) {
                    set_pixel(bgr, width, x, y, box);
                }
            }
        }

        // ── 顶部标签条：白字 + 实底色（独占区域：填充与顶边框均已跳过）──
        if (bar_w > 0 && lx2 <= x2) {
            for (int x = lx1 + t; x < lx2; x += 128) {
                if (x < 0 || x >= width) continue;
                const int ci = (x - lx1) / 8;
                const int bc = (x - lx1) % 8;
                const char ch = box.label[ci];
                if (ch < 0x20 || ch > 0x7E) continue;
                for (int r = 0; r < 16; ++r) {
                    const int y = ly1 + r;
                    if (y < 0 || y >= height) continue;
                    const uint8_t bits = FONT8X16[ch - 0x20][r];
                    uint8_t* p = bgr + (static_cast<size_t>(y) * width + x) * 3;
                    if (bits & (0x80 >> bc)) {
                        p[0] = 255; p[1] = 255; p[2] = 255;
                    } else {
                        p[0] = box.b; p[1] = box.g; p[2] = box.r;
                    }
                }
            }
        }
    }

    struct DrawGpuWorkspace {
        uint8_t* d_bgr = nullptr;
        GpuDrawBox* d_boxes = nullptr;
        size_t bgr_capacity = 0;
        size_t boxes_capacity = 0;

        ~DrawGpuWorkspace() {
            if (d_bgr) cudaFree(d_bgr);
            if (d_boxes) cudaFree(d_boxes);
        }
    };

    static thread_local DrawGpuWorkspace draw_gpu_ws;

    bool draw_boxes_gpu(uint8_t* bgr, int width, int height,
                        const GpuDrawBox* d_boxes, int num_boxes,
                        float alpha, cudaStream_t stream) {
        if (!bgr || width <= 0 || height <= 0 || !d_boxes || num_boxes <= 0) return true;

        bool is_internal_stream = false;
        if (stream == nullptr) {
            if (cudaStreamCreate(&stream) != cudaSuccess) return false;
            is_internal_stream = true;
        }

        const size_t bgr_bytes = static_cast<size_t>(height) * width * 3;
        const size_t boxes_bytes = sizeof(GpuDrawBox) * static_cast<size_t>(num_boxes);

        // d_boxes 指针可能是 host（调用方直接传 std::vector.data()）→ 自动上传
        cudaPointerAttributes box_attr{};
        const bool boxes_on_device =
            cudaPointerGetAttributes(&box_attr, d_boxes) == cudaSuccess &&
            box_attr.type == cudaMemoryTypeDevice;
        const GpuDrawBox* d_boxes_ptr;
        if (boxes_on_device) {
            d_boxes_ptr = d_boxes;
        } else {
            if (draw_gpu_ws.boxes_capacity < boxes_bytes) {
                GpuDrawBox* new_buf = nullptr;
                if (cudaMalloc(&new_buf, boxes_bytes) != cudaSuccess) {
                    // 分配失败：旧缓冲区仍有效，保持原状态，交由调用方回退 CPU 绘制
                    if (is_internal_stream) cudaStreamDestroy(stream);
                    return false;
                }
                if (draw_gpu_ws.d_boxes) cudaFree(draw_gpu_ws.d_boxes);
                draw_gpu_ws.d_boxes = new_buf;
                draw_gpu_ws.boxes_capacity = boxes_bytes;
            }
            cudaError_t upload_err =
                cudaMemcpyAsync(draw_gpu_ws.d_boxes, d_boxes, boxes_bytes, cudaMemcpyHostToDevice, stream);
            if (upload_err != cudaSuccess) {
                if (is_internal_stream) cudaStreamDestroy(stream);
                return false;
            }
            d_boxes_ptr = draw_gpu_ws.d_boxes;
        }

        // bgr 指针 device/host 检测（host 则上传，绘制后回拷，保证调用方看到结果）
        cudaPointerAttributes bgr_attr{};
        const bool bgr_on_device =
            cudaPointerGetAttributes(&bgr_attr, bgr) == cudaSuccess &&
            bgr_attr.type == cudaMemoryTypeDevice;

        uint8_t* d_bgr_ptr;
        if (bgr_on_device) {
            d_bgr_ptr = bgr;
        } else {
            if (draw_gpu_ws.bgr_capacity < bgr_bytes) {
                uint8_t* new_buf = nullptr;
                if (cudaMalloc(&new_buf, bgr_bytes) != cudaSuccess) {
                    if (is_internal_stream) cudaStreamDestroy(stream);
                    return false;
                }
                if (draw_gpu_ws.d_bgr) cudaFree(draw_gpu_ws.d_bgr);
                draw_gpu_ws.d_bgr = new_buf;
                draw_gpu_ws.bgr_capacity = bgr_bytes;
            }
            cudaError_t upload_err =
                cudaMemcpyAsync(draw_gpu_ws.d_bgr, bgr, bgr_bytes, cudaMemcpyHostToDevice, stream);
            if (upload_err != cudaSuccess) {
                if (is_internal_stream) cudaStreamDestroy(stream);
                return false;
            }
            d_bgr_ptr = draw_gpu_ws.d_bgr;
        }

        dim3 grid(static_cast<unsigned>(num_boxes));
        dim3 block(256);
        kernel_draw_boxes<<<grid, block, 0, stream>>>(
            d_bgr_ptr, width, height, d_boxes_ptr, num_boxes, alpha);

        cudaError_t launch_err = cudaGetLastError();
        cudaError_t copy_err = cudaSuccess;
        if (!bgr_on_device) {
            copy_err = cudaMemcpyAsync(bgr, d_bgr_ptr, bgr_bytes, cudaMemcpyDeviceToHost, stream);
        }
        cudaError_t sync_err = cudaStreamSynchronize(stream);
        if (is_internal_stream) cudaStreamDestroy(stream);
        return launch_err == cudaSuccess && copy_err == cudaSuccess && sync_err == cudaSuccess;
    }

    // ════════════════ NV12 设备侧就地绘制（Y/UV 双平面，颜色统一故同值良性竞争） ════════════════

    // BT.601 全幅：R/G/B -> Y/U/V（与 CPU draw_nv12.cpp 一致）
    __host__ __device__ inline void rgb_to_yuv_601(uint8_t r, uint8_t g, uint8_t b,
                                                   uint8_t* py, uint8_t* pu, uint8_t* pv) {
        const int ry = (66 * r + 129 * g + 25 * b + 128) >> 8;
        const int ub = (-38 * r - 74 * g + 112 * b + 128) >> 8;
        const int vr = (112 * r - 94 * g - 18 * b + 128) >> 8;
        *py = static_cast<uint8_t>(ry + 16 > 255 ? 255 : (ry + 16 < 0 ? 0 : ry + 16));
        *pu = static_cast<uint8_t>(ub + 128 > 255 ? 255 : (ub + 128 < 0 ? 0 : ub + 128));
        *pv = static_cast<uint8_t>(vr + 128 > 255 ? 255 : (vr + 128 < 0 ? 0 : vr + 128));
    }

    // 线程 = 单个 Y 像素；同色 chroma 竞争无害（每帧跨调用已同步）
    __device__ __forceinline__ void place_nv12(uint8_t* __restrict__ y, uint8_t* __restrict__ uv,
                                               int w, int h, int step_y, int step_uv,
                                               int px, int py, uint8_t yy, uint8_t uu, uint8_t vv) {
        if (px < 0 || py < 0 || px >= w || py >= h) return;
        y[static_cast<size_t>(py) * step_y + px] = yy;
        const int ux = px >> 1, uy = py >> 1;
        if (uy < 0 || uy >= h / 2 || ux < 0 || ux >= w / 2) return;
        uint8_t* p = uv + static_cast<size_t>(uy) * step_uv + static_cast<size_t>(ux) * 2;
        p[0] = uu; p[1] = vv;
    }

    __global__ void kernel_draw_rect_nv12(uint8_t* __restrict__ y, uint8_t* __restrict__ uv,
                                          int w, int h, int step_y, int step_uv,
                                          int x1, int y1, int x2, int y2, int thickness,
                                          uint8_t yy, uint8_t uu, uint8_t vv) {
        const int px = blockIdx.x * blockDim.x + threadIdx.x;
        const int py = blockIdx.y * blockDim.y + threadIdx.y;
        if (px >= w || py >= h) return;
        if (px < x1 || px >= x2 || py < y1 || py >= y2) return;
        const bool on_border = (py - y1 < thickness) || (y2 - py <= thickness) ||
                               (px - x1 < thickness) || (x2 - px <= thickness);
        if (on_border) place_nv12(y, uv, w, h, step_y, step_uv, px, py, yy, uu, vv);
    }

    // 点到线段距离（Bresenham 厚线近似：距离 <= thickness/2 视为在线）
    __device__ __forceinline__ float dist_to_segment(float px, float py,
                                                     float ax, float ay, float bx, float by) {
        const float vx = bx - ax, vy = by - ay;
        const float wx = px - ax, wy = py - ay;
        const float len2 = vx * vx + vy * vy;
        float t = len2 > 1e-9f ? (wx * vx + wy * vy) / len2 : 0.0f;
        t = fminf(fmaxf(t, 0.0f), 1.0f);
        const float cx = ax + t * vx, cy = ay + t * vy;
        const float dx = px - cx, dy = py - cy;
        return sqrtf(dx * dx + dy * dy);
    }

    __global__ void kernel_draw_polygon_nv12(uint8_t* __restrict__ y, uint8_t* __restrict__ uv,
                                             int w, int h, int step_y, int step_uv,
                                             const float* __restrict__ xs, const float* __restrict__ ys,
                                             int npts, float half_thick,
                                             uint8_t yy, uint8_t uu, uint8_t vv) {
        const int px = blockIdx.x * blockDim.x + threadIdx.x;
        const int py = blockIdx.y * blockDim.y + threadIdx.y;
        if (px >= w || py >= h) return;
        float min_d = 1e30f;
        for (int i = 0; i < npts; ++i) {
            const int j = (i + 1) % npts;
            const float d = dist_to_segment(static_cast<float>(px), static_cast<float>(py),
                                            xs[i], ys[i], xs[j], ys[j]);
            if (d < min_d) min_d = d;
        }
        if (min_d <= half_thick) place_nv12(y, uv, w, h, step_y, step_uv, px, py, yy, uu, vv);
    }

    __global__ void kernel_draw_points_nv12(uint8_t* __restrict__ y, uint8_t* __restrict__ uv,
                                            int w, int h, int step_y, int step_uv,
                                            const float* __restrict__ xs, const float* __restrict__ ys,
                                            int npts, int radius, int radius2,
                                            uint8_t yy, uint8_t uu, uint8_t vv) {
        const int px = blockIdx.x * blockDim.x + threadIdx.x;
        const int py = blockIdx.y * blockDim.y + threadIdx.y;
        if (px >= w || py >= h) return;
        for (int i = 0; i < npts; ++i) {
            const int dx = px - static_cast<int>(xs[i]);
            const int dy = py - static_cast<int>(ys[i]);
            if (dx * dx + dy * dy <= radius2) {
                place_nv12(y, uv, w, h, step_y, step_uv, px, py, yy, uu, vv);
                return;
            }
        }
    }

    // 文本 blit：每字符一个 block，thread 映射到 8x16 字模位，内部按 scale 展开写像素
    __global__ void kernel_draw_text_nv12(uint8_t* __restrict__ y, uint8_t* __restrict__ uv,
                                          int w, int h, int step_y, int step_uv,
                                          const char* __restrict__ text, int base_x, int base_y,
                                          int font_size,
                                          uint8_t yy, uint8_t uu, uint8_t vv) {
        const int ci = blockIdx.x;
        const unsigned char ch = static_cast<unsigned char>(text[ci]);
        if (ch < 0x20 || ch > 0x7E) return;
        const int fx = threadIdx.x;          // 0..7（字模列）
        const int fy = threadIdx.y;          // 0..15（字模行）
        const uint8_t bits = FONT8X16[ch - 0x20][fy];
        if (!(bits & (0x80U >> fx))) return;
        const int x0 = base_x + ci * 8 * font_size + fx * font_size;
        const int y0 = base_y + fy * font_size;
#pragma unroll
        for (int sy = 0; sy < font_size; ++sy) {
            const int py = y0 + sy;
            if (py < 0 || py >= h) continue;
#pragma unroll
            for (int sx = 0; sx < font_size; ++sx) {
                const int px = x0 + sx;
                if (px < 0 || px >= w) continue;
                place_nv12(y, uv, w, h, step_y, step_uv, px, py, yy, uu, vv);
            }
        }
    }

    // ── host 包装：创建/复用流，网格遍历绘制区域像素 ──
    static cudaStream_t acquire_stream(cudaStream_t user, bool* owned) {
        *owned = false;
        if (user) return user;
        *owned = true;
        cudaStream_t s = nullptr;
        cudaStreamCreateWithFlags(&s, cudaStreamNonBlocking);
        return s;
    }

    bool draw_rect_nv12_gpu(uint8_t* y, uint8_t* uv, int w, int h, int step_y, int step_uv,
                            float xx, float yo, float rw, float rh,
                            uint8_t r, uint8_t g, uint8_t b, int thickness,
                            cudaStream_t stream) {
        if (!y || (!uv && h > 1) || w <= 0 || h <= 0) return false;
        uint8_t yy, uu, vv; rgb_to_yuv_601(r, g, b, &yy, &uu, &vv);
        int x1 = static_cast<int>(xx), y1 = static_cast<int>(yo);
        int x2 = static_cast<int>(xx + rw), y2 = static_cast<int>(yo + rh);
        if (thickness <= 0) thickness = 1;
        bool owned; cudaStream_t s = acquire_stream(stream, &owned);
        dim3 block(32, 4);
        dim3 grid((w + block.x - 1) / block.x, (h + block.y - 1) / block.y);
        cudaError_t err = cudaSuccess;
        if (x2 > x1 && y2 > y1) {
            kernel_draw_rect_nv12<<<grid, block, 0, s>>>(
                y, uv, w, h, step_y, step_uv, x1, y1, x2, y2, thickness, yy, uu, vv);
            err = cudaGetLastError();
        }
        cudaError_t sync = cudaStreamSynchronize(s);
        if (owned) cudaStreamDestroy(s);
        return err == cudaSuccess && sync == cudaSuccess;
    }

    bool draw_polygon_nv12_gpu(uint8_t* y, uint8_t* uv, int w, int h, int step_y, int step_uv,
                               const float* xs, const float* ys, int npts,
                               uint8_t r, uint8_t g, uint8_t b, int thickness,
                               cudaStream_t stream) {
        if (!y || (!uv && h > 1) || w <= 0 || h <= 0 || npts < 3) return false;
        uint8_t yy, uu, vv; rgb_to_yuv_601(r, g, b, &yy, &uu, &vv);
        if (thickness <= 0) thickness = 1;
        bool owned; cudaStream_t s = acquire_stream(stream, &owned);
        float* d_xs = nullptr; float* d_ys = nullptr;
        const size_t sz = static_cast<size_t>(npts) * sizeof(float);
        cudaError_t err = cudaMalloc(&d_xs, sz);
        if (err == cudaSuccess) err = cudaMalloc(&d_ys, sz);
        if (err == cudaSuccess) err = cudaMemcpyAsync(d_xs, xs, sz, cudaMemcpyHostToDevice, s);
        if (err == cudaSuccess) err = cudaMemcpyAsync(d_ys, ys, sz, cudaMemcpyHostToDevice, s);
        if (err == cudaSuccess) {
            const float half = thickness * 0.5f;
            dim3 block(32, 4);
            dim3 grid((w + block.x - 1) / block.x, (h + block.y - 1) / block.y);
            kernel_draw_polygon_nv12<<<grid, block, 0, s>>>(
                y, uv, w, h, step_y, step_uv, d_xs, d_ys, npts, half, yy, uu, vv);
            err = cudaGetLastError();
        }
        cudaError_t sync = cudaStreamSynchronize(s);
        if (d_xs) cudaFree(d_xs);
        if (d_ys) cudaFree(d_ys);
        if (owned) cudaStreamDestroy(s);
        return err == cudaSuccess && sync == cudaSuccess;
    }

    bool draw_points_nv12_gpu(uint8_t* y, uint8_t* uv, int w, int h, int step_y, int step_uv,
                              const float* xs, const float* ys, int npts,
                              uint8_t r, uint8_t g, uint8_t b, int radius,
                              cudaStream_t stream) {
        if (!y || (!uv && h > 1) || w <= 0 || h <= 0 || npts < 1) return false;
        uint8_t yy, uu, vv; rgb_to_yuv_601(r, g, b, &yy, &uu, &vv);
        if (radius <= 0) radius = 1;
        const int radius2 = radius * radius;
        bool owned; cudaStream_t s = acquire_stream(stream, &owned);
        float* d_xs = nullptr; float* d_ys = nullptr;
        const size_t sz = static_cast<size_t>(npts) * sizeof(float);
        cudaError_t err = cudaMalloc(&d_xs, sz);
        if (err == cudaSuccess) err = cudaMalloc(&d_ys, sz);
        if (err == cudaSuccess) err = cudaMemcpyAsync(d_xs, xs, sz, cudaMemcpyHostToDevice, s);
        if (err == cudaSuccess) err = cudaMemcpyAsync(d_ys, ys, sz, cudaMemcpyHostToDevice, s);
        if (err == cudaSuccess) {
            dim3 block(32, 4);
            dim3 grid((w + block.x - 1) / block.x, (h + block.y - 1) / block.y);
            kernel_draw_points_nv12<<<grid, block, 0, s>>>(
                y, uv, w, h, step_y, step_uv, d_xs, d_ys, npts, radius, radius2, yy, uu, vv);
            err = cudaGetLastError();
        }
        cudaError_t sync = cudaStreamSynchronize(s);
        if (d_xs) cudaFree(d_xs);
        if (d_ys) cudaFree(d_ys);
        if (owned) cudaStreamDestroy(s);
        return err == cudaSuccess && sync == cudaSuccess;
    }

    bool draw_text_nv12_gpu(uint8_t* y, uint8_t* uv, int w, int h, int step_y, int step_uv,
                            float xx, float yo, const char* text,
                            uint8_t r, uint8_t g, uint8_t b, int font_size,
                            cudaStream_t stream) {
        if (!y || !text || w <= 0 || h <= 0) return false;
        if (font_size <= 0) font_size = 1;
        const int len = static_cast<int>(std::strlen(text));
        if (len <= 0) return true;
        uint8_t yy, uu, vv; rgb_to_yuv_601(r, g, b, &yy, &uu, &vv);
        const int base_x = static_cast<int>(xx), base_y = static_cast<int>(yo);
        bool owned; cudaStream_t s = acquire_stream(stream, &owned);
        cudaError_t err = cudaSuccess;
        char* d_text = nullptr;
        if (len > 0) {
            const size_t sz = static_cast<size_t>(len + 1);
            err = cudaMalloc(&d_text, sz);
            if (err == cudaSuccess) err = cudaMemcpyAsync(d_text, text, sz, cudaMemcpyHostToDevice, s);
        }
        if (err == cudaSuccess) {
            dim3 block(8, 16);
            kernel_draw_text_nv12<<<len, block, 0, s>>>(
                y, uv, w, h, step_y, step_uv, d_text, base_x, base_y, font_size, yy, uu, vv);
            err = cudaGetLastError();
        }
        cudaError_t sync = cudaStreamSynchronize(s);
        if (d_text) cudaFree(d_text);
        if (owned) cudaStreamDestroy(s);
        return err == cudaSuccess && sync == cudaSuccess;
    }
}
